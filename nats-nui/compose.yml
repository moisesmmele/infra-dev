services:
  nats-nui:
    image: ghcr.io/nats-nui/nui:latest
    container_name: ${NATS_NUI_CONTAINER_NAME:-nats-nui}
    ports:
      - "${NATS_NUI_PORT:-31311}:31311"
    volumes:
      - ./mounts/db:/db
    restart: unless-stopped
    networks:
      - dev_net

networks:
  dev_net:
    external: true
    name: ${DOCKER_NETWORK_NAME}
